import {Point} from '../model/Point'
/* 두번 이하로 꺾는지 확인 */
export function checkTurnCount(p1, p2){
    // path : 시작 좌표, 꺾는 좌표, 종료 좌표
    // canRemove : 없앨 수 있는지 여부
    var path = [];
    return [path, canRemove];
}